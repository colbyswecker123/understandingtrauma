# Minimal Homepage + Fixed Header

This version corrects two things:

1. The home page no longer shows the large extra cards/buttons:
   - Support tools
   - Destress
   - Faith
   - Store
   - Support
   - Calm mode

Those pages still exist, but they are accessed through the hamburger menu.

2. The header is now fixed to the viewport instead of sticky inside the page shell.
   This should keep it frozen at the top on mobile while the page scrolls.

Test with:

https://understandingtrauma.org/?v=11
